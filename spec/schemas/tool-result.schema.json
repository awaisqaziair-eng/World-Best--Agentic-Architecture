{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://polymath.dev/spec/tool-result.schema.json",
  "title": "ToolResult",
  "type": "object",
  "properties": {
    "call_id": {"type": "string", "description": "Id of the ToolCall this answers."},
    "name": {"type": "string"},
    "output": {"type": "string", "description": "Model-facing text, already budgeted (head+tail clipped, spill path noted)."},
    "is_error": {"type": "boolean", "description": "True for tool faults (bad args, crash, timeout). A non-zero process exit is NOT an error."},
    "duration_s": {"type": "number", "minimum": 0},
    "truncated": {"type": "boolean"},
    "meta": {"type": "object", "description": "Tool-specific structured data (exit_code, cwd, coercions, ...)."}
  },
  "required": ["call_id", "name", "output", "is_error"]
}
